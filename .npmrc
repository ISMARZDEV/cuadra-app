# React Native + Metro expect a flat node_modules. pnpm's default isolated layout
# breaks RN libraries (phantom deps, e.g. react-native-css-interop/jsx-runtime).
# Expo's official guidance for pnpm monorepos: use a hoisted node-linker.
node-linker=hoisted
