// Font-family names as registered by `useFonts()` in app/_layout.tsx — a typo in a raw string
// silently falls back to the system font with no error, so every money-figure text references
// these constants instead of retyping "Akshar_600SemiBold" by hand.
export const AKSHAR_MEDIUM = "Akshar_500Medium";
export const AKSHAR_SEMIBOLD = "Akshar_600SemiBold";
